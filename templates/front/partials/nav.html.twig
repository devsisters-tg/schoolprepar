<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
        <a class="navbar-brand fw-bold fs-4" href="{{ path('home') }}">
            <i class="bi bi-mortarboard-fill me-2"></i>School<span>Prepar</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMain">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMain">
            <ul class="navbar-nav ms-auto gap-1 align-items-lg-center">
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.pathInfo == '/' ? 'active' : '' }}" href="{{ path('home') }}">
                        <i class="bi bi-house me-1"></i>Accueil
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.pathInfo starts with '/filieres' ? 'active' : '' }}" href="{{ path('filieres') }}">
                        <i class="bi bi-book me-1"></i>Filières
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link px-3 {{ app.request.pathInfo starts with '/etablissements' ? 'active' : '' }}" href="{{ path('etablissements') }}">
                        <i class="bi bi-building me-1"></i>Établissements
                    </a>
                </li>

                {% if app.user %}
                    {# Utilisateur connecté #}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item ms-2">
                            <a class="btn btn-warning btn-sm px-3 text-dark fw-semibold" href="{{ path('admin_dashboard') }}">
                                <i class="bi bi-shield-lock me-1"></i>Admin
                            </a>
                        </li>
                    {% endif %}
                    <li class="nav-item dropdown ms-2">
                        <a class="btn btn-outline-light btn-sm px-3 dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                            <i class="bi bi-person-circle me-1"></i>{{ app.user.nomComplet }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <span class="dropdown-item-text text-muted small">
                                    {% if is_granted('ROLE_ADMIN') %}
                                        <i class="bi bi-shield-fill-check text-warning me-1"></i>Administrateur
                                    {% else %}
                                        <i class="bi bi-person-check me-1"></i>Utilisateur
                                    {% endif %}
                                </span>
                            </li>
                            <li><hr class="dropdown-divider"></li>
                            <li>
                                        <li>
                        <a class="dropdown-item" href="{{ path('app_profile') }}">
                            <i class="bi bi-person me-2"></i>Mon profil
                        </a>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                    <li>
                        <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                    <i class="bi bi-box-arrow-right me-2"></i>Déconnexion
                                </a>
                            </li>
                        </ul>
                    </li>
                {% else %}
                    {# Visiteur non connecté #}
                    <li class="nav-item ms-1">
                        <a class="btn btn-outline-light btn-sm px-3" href="{{ path('app_login') }}">
                            <i class="bi bi-box-arrow-in-right me-1"></i>Connexion
                        </a>
                    </li>
                    <li class="nav-item ms-1">
                        <a class="btn btn-primary btn-sm px-3" href="{{ path('app_register') }}">
                            <i class="bi bi-person-plus me-1"></i>S'inscrire
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
